# 🔄 项目交接报告 —— 偷蛋长跑（egg-runner）

> **给接手的 AI**：请从头到尾读完这一份，再决定动手改什么。
> 这份文档记录了 2026-09-12 一整天的设计推演、四轮真人试玩、一轮快修、以及所有踩过的坑。
> **很多坑是"看起来对、实际翻车"的，凭直觉改大概率会重复踩。**

---

## 零、⚠️ 交接须知（2026-09-13 更新，**先读这段**）

### 代码状态：已推 GitHub，工作区干净

- 远程 `main` 与本地一致，最新提交 **`62b50dd`**
- 本地无未提交改动（仓库根只有一个临时诊断 txt 被 gitignore）

### 玩家存档有两个，**别搞混，更别删**

| 存档 | 位置 | 内容 |
|---|---|---|
| **当前在玩** | `<仓库根>/Godot/app_userdata/偷蛋长跑/egg_runner_save.json` | 速度 Lv5 / 343 金币 / 最远 162 m / 农场 5 只 |
| **更老的富档** | `../egg-runner/Godot/app_userdata/偷蛋长跑/egg_runner_save.json` | 速度 **Lv19** / **128 万金币** / 最远 **1165 m** / 农场 **12 只** / 隐蔽 Lv12 / 跑鞋 Lv16 / 披风 5 / 喷气 5 |

- ⚠️ **`../egg-runner/` 这个兄弟目录千万别删。** 它的代码不跟进（是旧副本），
  但里面存着玩家**更早的富档** —— 就是"进度归零"事件里丢掉的那一份。
  `../_verify_clone/`、`../_chk_a/` 里也各有一份存档，同理别当垃圾清掉。
- 两份都已带时间戳备份在 `steal_egg/Godot/app_userdata/偷蛋长跑/`：
  `egg_runner_save.json.bak-*-Lv19-旧档` 与 `...-Lv5-当前在玩`
- ⏳ **待玩家拍板**：要不要把 Lv19 旧档恢复成正档（会覆盖当前 Lv5 进度）。
  **恢复前必须先关掉游戏窗口**，否则运行中的游戏会把它覆盖回去。

### 存档路径为什么**不能**用 `user://`（血泪，务必看懂）

本机启动环境里 **`APPDATA` 是空的**，Godot 于是把 `user://` 拼成
**相对路径** `./Godot/app_userdata/偷蛋长跑` —— 而相对路径由**进程工作目录**决定。
换个目录启动 = 另一份（空的）存档。**玩家的 Lv14 档就是这么"消失"的。**

- 现在 `SaveManager.save_path()` 显式锚定 `res://` 下的**绝对路径**（导出 .pck 才退回 `user://`）
- 怀疑复发：跑 `tools/path_probe.gd`，**从两个不同工作目录各跑一次，打印的路径必须一致**
- 启动日志里会打印 `[存档] 位置：...`，一眼就能看出有没有漂

### 怎么启动（用户要能自己双击）

工程根下有 `play.cmd`（直接玩）/ `editor.cmd`（编辑器打开）。
工程已登记进 `%APPDATA%/Godot/projects.cfg`，Godot 项目管理器里能看到。

> ⚠️ 项目路径**含空格**。手工命令行启动**必须给路径加引号**，
> 否则 Godot 报 `Invalid project path specified` 后**瞬间退出** ——
> 用户会描述成"闪退"。**先排除这个，再去查代码。**

### ✅ 已修：被抓惩罚会把起始阶位打回 T1「草鸡」

用户明确说过**"别出草鸡"**。原实现会被抓越多、门开得越浅，最后掉回 T1。
已修（提交见 `git log`）：`CreatureDB.MIN_START_TIER = 2` 兜底，
`start_tier_for()` 末尾 `maxi(t, MIN_START_TIER)`。

> ⚠️ **别把它"顺手改成"看干净速度（`get_clean_speed()`）。**
> 这是我差点犯的错，实测过会变成更严重的 bug：
>
> `start_tier` 是跑道上的**最低阶**（`tier_at()` 只会往上加），
> 开在哪一阶，你就**只能**面对那一阶的怪，跑不过也躲不开。
> 所以它必须等于「你**当前真的**跑得过的那一阶」。
>
> 实测本档（速度 Lv5 / 跑鞋 Lv3 / 被抓 10 次）：
> | 基准 | 起始阶 | 该阶怪速 | 实际速度 | 结果 |
> |---|---|---|---|---|
> | 干净速度 25.39 | **T6** | 22.3 | 18.72 | ❌ 跑不过，必被抓 |
> | 实际速度 18.72 | **T5** | 17.1 | 18.72 | ✅ 跑得过 |
>
> 用干净速度会把惩罚期的玩家扔进跑不过的阶位 → 必被抓 → 惩罚更深 →
> **死循环**，比原来的 T1 问题严重得多。
>
> 正确的边界是：**允许随惩罚变浅，但不许低于 T2。**

**回归方式**：`--tier`（场景自检，因为 `--script` 用不了 autoload）。
它会断言"惩罚封底时起点阶 ≥ T2"，并打印一条诊断
（起始阶怪速 vs 实际速度，看惩罚是否生效）。

顺带一提：被抓惩罚**不是永久不可逆** —— `GameState.heal()`
（花金币，一次抹掉 `HEAL_CATCHES = 3` 次）可以恢复。农场界面上有按钮。

---

## 一、30 秒速览

| 项目 | 说明 |
|---|---|
| **游戏名** | 偷蛋长跑（内部代号 `egg-runner`） |
| **类型** | 2.5D 无限跑道 · 偷蛋跑酷 · 数值养成 |
| **引擎** | Godot **4.7.2** stable，`gl_compatibility` 渲染，1280×720 |
| **语言** | GDScript |
| **当前状态** | **可玩，外循环已闭合**，正在打磨"数值膨胀感" |
| **代码量** | 12 个 `.gd` + 6 个 `.tscn`，约 330 KB |
| **当前分支** | 直接推 `main` |

**一句话玩法**：
一条越跑越危险的跑道，沿途怪物在沉睡。偷走它们的蛋 → 怪物苏醒追你 →
被追上就抢回最贵的蛋并**永久降速** → 带着蛋跑回起点才算数 →
把蛋养在农场产钱 → 钱用来买跑鞋/锻炼/装备 → 变得更快 → 能跑更深偷更贵的蛋。

---

## 二、这是两个项目，别搞混

仓库里有两个 Godot 项目，**只有一个是活的**：

| 项目 | 路径 | 状态 | 说明 |
|---|---|---|---|
| `egg-runner` | **本仓库根目录**（即 `steal_egg/`） | ✅ **活跃** | 当前在做的。2.5D 无限跑道偷蛋 |
| `archive/egg-thief-sim` | 本仓库 `archive/` 下 | ❌ 停摆 | v1，2D 关卡制潜行。用户明确推翻 |

> ⚠️ **别被同名目录绕晕**：本仓库的**上一级**还有几个**兄弟目录**
> （`../egg-runner/`、`../_verify_clone/`、`../_chk_a/`），
> 它们是历史副本，**代码不跟进，改代码只改本仓库**。
> 但它们各自存着一份玩家存档 —— **不要删**，详见第零节。

### 为什么 v1 被推翻

v1 是"2D 俯视角关卡制潜行"：一张巢穴地图、守卫视野锥、警戒值、负重系统、
10 生态阶 × 40 品种 × 4 品质 = 160 图鉴条目、孵化/献祭/伙伴陪同……

**用户玩都没玩完就说方向错了**，原话：

> "方向错了，先这样，做2.5D，然后一条跑道，走得越远，怪物就越高级，
> 这些怪物的速度也就越快，他们都在沉睡，你偷了他们的蛋，他们就会醒来追你，
> 追到你的话，蛋就会被抢回去，并且你的速度也会被永久降低，
> 把这些蛋偷回来以后，可以放到自己的农场养着，他们会产生经济价值，
> 经济价值可以用来提升速度以及隐蔽性以用来投入偷更高级的蛋"

**这段原话就是 v2 的完整需求，逐句都实现了。** 接手时请以这段话为准绳。

> ⚠️ **v1 的教训**：内容堆到 160 个图鉴条目、30 个关卡、25-35 小时流程，
> 全部是在**没有一次真人试玩**的情况下做的。结果用户上手就说方向错了。
> **先做能玩的最小闭环，让人玩，再补内容。**

---

## 三、立刻跑起来

### 环境事实（已验证，不要再去猜）

```
引擎真身：H:/Godot_v4.7.2-stable_win64.exe/Godot_v4.7.2-stable_win64.exe
          ↑ 注意：H:/Godot_v4.7.2-stable_win64.exe 是个【目录】不是 exe
          这是便携解压包，真 exe 在它里面（约 180MB）
引擎不在 PATH，必须写全路径
Godot 用户配置：C:/Users/433Somaz/AppData/Roaming/Godot/
```

### 启动

```bash
cd <仓库根>
"H:/Godot_v4.7.2-stable_win64.exe/Godot_v4.7.2-stable_win64.exe" --path . --resolution 1280x720
```

主场景是 `scenes/main.tscn`。**注意：从 2026-09-12 快修起，主场景已从 `farm.tscn` 改为 `main.tscn`。**
现在是**单场景**：跑道（z 负方向延伸）与撤离点后的农场/商店（z 正方向）**在同一个场景里**，
往回跑过撤离点就直接走到店铺和兽栏，**不再切场景**。`scenes/farm.tscn` 仍保留但已不是主场景（旧 UI 面板）。

### 无窗口自检（改完代码先跑这个）

```bash
GODOT="H:/Godot_v4.7.2-stable_win64.exe/Godot_v4.7.2-stable_win64.exe"

# 默认主场景（跑道 + 农场）
"$GODOT" --path . --headless --quit-after 180

# 单独验证跑道场景，--jump 跳到指定距离，--diag 打印阶位分布 + 最近怪距离
"$GODOT" --path . --headless --main-scene res://scenes/main.tscn --jump 900 --diag

# 逐阶查看十套怪物模型（按 1-9、0 跳阶）
"$GODOT" --path . --main-scene res://scenes/main.tscn --gallery 10

# 商店/农场自检（快照-购买-还原，不会污染存档）
"$GODOT" --path . --headless --main-scene res://scenes/main.tscn --shop

# 撤离链路自检：背包→过线入库→卖蛋 / 入农场→再卖蛋
"$GODOT" --path . --headless --main-scene res://scenes/main.tscn --esc

# 追兵边界自检：怪会不会追进农场撞人（要跑 1200 帧，别缩短）
"$GODOT" --path . --headless --main-scene res://scenes/main.tscn --chase --quit-after 1200

# 输入绑定 + 移动方向是否跟随相机
"$GODOT" --path . --headless --script res://tools/input_test.gd

# 十阶模型建模自检
"$GODOT" --path . --headless --script res://tools/model_test.gd

# 蛋品曲线自检（品质是否真的随距离变稀有，统计断言）
"$GODOT" --path . --headless --script res://tools/quality_test.gd
```

**健康输出长这样**（出现 `[自检]` 且无 ERROR 即通过）：
```
[自检] 起点阶 T2 ｜ 距离 900 m ｜ 当前阶 T4 ｜ 沉睡怪 84 只（兽栏 1 只不计）｜ 蛋 84 颗
       ｜ 最近怪 18 m ｜ 阶位分布 { 2: 5, 1: 1, 3: 13, 4: 6, 5: 18, 6: 25, 7: 16 }
```

> `--headless` 下会有一条 `Can't create shader cache folder` 的警告，**无害，可忽略**。
>
> **所有自检都不会写你的存档。** 自检一开始就把 `SaveManager.test_mode` 打开，
> 此时 `save()` 是空操作。跑完存档逐字节不变——这一点每次改完都值得对一下。
> 之所以从"跑完再还原"改成"直接禁写"：中途报错、进程被杀、玩家同时在玩，
> 任何一样都会把测试数据留在档里（真的把玩家的档写成过 `coins: 999479`）。

### 操作（快修后）

| 键 | 作用 |
|---|---|
| `W/A/S/D` **或** 方向键 | 移动（**跟着视角走**，见下） |
| **鼠标移动** | 自由视角（轨道相机，不需要按住任何键） |
| **鼠标滚轮** | 拉近/拉远（3.0 – 24.0） |
| `E` | 偷蛋 / 在店铺前交互（读条 1.2 秒，期间不能动） |
| `Shift` | 蹲行（偷蛋波及半径 12 → 6） |
| `Tab` | 释放/捕获鼠标（松开鼠标指针，方便操作窗口） |
| `Esc` | 第一次按下释放鼠标；鼠标已释放时再按一次 → 切到旧农场 UI 面板 `farm.tscn` |

> **鼠标视角默认是"锁定指针"模式**：进游戏鼠标就隐藏并锁定在窗口中心，移动鼠标即转视角。
> 想拿回指针按 `Tab` 或 `Esc`。

> ⚠️ **移动是相对相机的**：按 `W` 永远是"往画面里走"，不是"往世界 −Z 走"。
> 换算逻辑在 `scripts/core/move_math.gd`，有 8 个断言守着（见 `tools/input_test.gd`）。
> 这块如果写回世界坐标，转一下视角按 W 就会横着走——**玩家会直接说"人物不听我的"**。

---

## 四、项目结构

```
仓库根/
├── HANDOFF.md              ★ 你正在读的
├── README.md               简短入口
├── project.godot           Godot 工程配置（autoload 在此）
├── scenes/
│   ├── main.tscn           ★ 主场景：跑道 + 撤离点后的农场/商店（单场景）
│   ├── farm.tscn           旧农场 UI 面板（已非主场景，Esc 二次按下才进）
│   └── run/
│       ├── creature.tscn   怪物
│       ├── egg.tscn        蛋
│       ├── landmark.tscn   远处巨型生物（纯视觉）
│       └── speed_fx.tscn   速度线 + 尘土粒子
├── scripts/
│   ├── core/
│   │   ├── game_events.gd  信号总线
│   │   ├── game_state.gd   ★ 所有数值都在这
│   │   ├── move_math.gd    ★ 输入→世界方向（纯函数，可自检）
│   │   ├── main.gd         跑道主逻辑 + 轨道相机 + HUD + 商店交互
│   │   └── save_manager.gd 存档（user://egg_runner_save.json）+ test_mode
│   ├── data/
│   │   └── creature_db.gd  ★ 怪物/阶位数据表 + 世界尺度/相机公式
│   ├── farm/
│   │   └── farm.gd         旧农场 UI 逻辑
│   └── run/
│       ├── player.gd       玩家（移动 + 农场区边界）
│       ├── creature.gd         怪物状态机（含兽栏漫游 / 陈列自转）
│       ├── creature_model.gd   ★ 十阶身体蓝图（每阶一套，不是换色）
│       ├── farm_zone.gd        ★ 撤离点后的农场 + 六个店铺摊位 + 兽栏
│       ├── egg.gd              蛋
│       ├── track.gd            跑道生成
│       ├── landmark.gd         远处巨兽
│       └── speed_fx.gd         速度特效
├── tools/
│   ├── model_test.gd       建模自检（--script 跑，逐阶构建十套模型）
│   ├── quality_test.gd     蛋品曲线自检（--script 跑，统计断言）
│   ├── input_test.gd       输入自检（--script 跑，绑定 + 移动方向数学）
│   └── _probe.gd           最小 --script 冒烟测试
├── docs/                    设计文档（见下）
└── archive/egg-thief-sim/   v1 停摆项目
```

### 文档索引

| 文档 | 内容 |
|---|---|
| `docs/01-核心循环.md` | 玩法闭环、螺旋模型、起始阶位系统 |
| `docs/02-数值表.md` | 十阶怪物完整数值、成长曲线、经济 |
| `docs/03-装备与速度感.md` | 速度特效、数值虚假化、四源提升 |
| `docs/04-试玩反馈与迭代记录.md` | **四轮真人反馈逐条对照** |
| `docs/05-环境与运行手册.md` | 引擎路径、headless 自检、调试技巧、全部命令行开关 |
| `docs/06-技术陷阱清单.md` | **GDScript / Godot 4 踩坑集，改代码前必读** |
| `docs/07-待办与已知问题.md` | 没做完的、已知的 bug |
| `docs/08-怪物建模与陈列模式.md` | 十阶身体蓝图、`--gallery` 陈列、建模自检 |
| `docs/09-农场区与自由视角.md` | **农场区布局、轨道相机、兽栏漫游、快修四个坑** |

---

## 五、核心机制（当前实现）

### 主闭环

```
        ┌──────────────────────────────────────┐
        │                                      │
        ▼                                      │
   农场买装备 ──► 速度↑ ──► 起始阶位更深 ──► 蛋更值钱
        ▲                                      │
        │                                      ▼
   钱更多 ◄── 农场产钱 ◄── 养蛋 ◄── 带着蛋逃回起点
```

**唯一的刹车**：被追上 → 抢走最贵一颗蛋 + `速度 ×= 0.97`（永久，要花钱治）。
没有这个刹车，整个循环就是无风险滚雪球。

### 撤离点后的农场区（2026-09-12 快修新增）

用户原话：

> "我想加入鼠标自由视角，然后不分局外，撤离点后面就是可以升级的店铺以及可以看到自己养的生物"

**核心决定：不再分场景。** 跑到 `z=0`（撤离点）之后继续往回走，就进入农场区。
玩家在同一场景里从"偷蛋"平滑走到"花钱/看宠物"，没有加载、没有切换。

布局（`scripts/run/farm_zone.gd`，全在 z 正方向）：

```
z 负 ←──────── 跑道（越深怪越高级）──────── z=0 撤离点 ────► z 正（农场区）

  z=10  六个店铺摊位，x 从 -18 到 19.5，等距排开：
        [锻炼] [跑鞋] [披风] [喷气] [入农场] [卖蛋]
  z=46  兽栏：半径 15 的圆形围栏，养的怪在里面自由漫步
  z=74  农场区尽头
```

| 常量 | 值 | 说明 |
|---|---|---|
| `HALF_W` | 26.0 | 农场区左右半宽 |
| `STATION_Z` | 10.0 | 摊位所在 z |
| `PEN_Z` / `PEN_R` | 46.0 / 15.0 | 兽栏中心 / 半径 |
| `Z_FAR` | 74.0 | 尽头 |
| `INTERACT_RANGE` | 5.0 | 交互距离 |
| `MAX_PEN_CREATURES` | 20 | 兽栏最多显示 20 只（超过只显示，不影响数据） |

**交互**：走近任一摊位（< 5 米）→ 屏幕提示 → 按 `E` 买/用。
**兽栏里的怪**用**黄金角 2.399963 散射**排布，半径 `sqrt(i/n) × PEN_R`，
每只自己随机挑一个 `_wander_to` 慢慢走过去，边走边做各自的动画（腿摆/分节波/扇翼/悬浮）。

### 安全线（`CreatureDB.SAFE_LINE = -12`）

**农场区是安全区。** 追兵追到这条线就必须回头，物理上也不许越线。

这条线**必须是同一个常量**，因为有三个地方要用它：

| 用处 | 位置 | 少了会怎样 |
|---|---|---|
| 蛋自动入库 | `main.gd _check_escape()` | 蛋带不回来 |
| 追兵放弃 | `creature.gd _tick_chase()` | 怪追进店铺门口撞人 |
| 追兵位置夹取 | `creature.gd _tick_chase()` | 高速怪一步跨过线 |

以前这三处各写各的（`-12` / `1500` / 不夹），结果就是用户报的：
**"怪物都追到撤离点里面来撞我了"**，而且连带第二个症状——
玩家在自家门口被撞掉刚入库的蛋，然后去卖蛋发现是空的，
于是以为"游戏把我蛋弄丢了"。

> ⚠️ **要改安全区范围，只改 `SAFE_LINE` 一处，别在别处再写一遍数字。**
> 追兵放弃和蛋入库**必须同一帧发生**：只做一半，玩家就会在自家门口被抢第二次。

线在地面上有实体标记（发光的横线 + `安全线 · 过此线追兵回头` 的 Label3D），
跨线时还会弹一次 `已进入撤离区——追兵到此为止` 的提示。

> **和旧 `farm.tscn` 的关系**：旧的 UI 列表式农场**没删**，仍可通过 `Esc` 二次按下进入。
> 新农场区是**世界内的可视化版本**，两者读同一份 `GameState`，不冲突。

### 四个速度来源（三种算法）

最终速度 = `(9.0 + 跑鞋×2.5) × 1.09^锻炼 × 0.97^被抓次数 × 喷气倍率`

| 来源 | 算法 | 上限 | 成本 | 定位 |
|---|---|---|---|---|
| **锻炼** `speed_level` | 乘 `×1.09^Lv` | Lv30（×13.3） | `40 × 1.62^Lv` | 后期主力 |
| **跑鞋** `shoe_level` | 加 `+2.5/级` | Lv20（+50） | `80 × 1.70^Lv` | 前期立竿见影 |
| **隐形披风** `gear_cloak` | 苏醒延迟 `+15%/级` | Lv5（×1.75） | `200 × 2.20^Lv` | 保命，不加速度 |
| **喷气背包** `gear_jet` | 开局 2.5s `+25%/级` | Lv5（×2.25） | `200 × 2.20^Lv` | 偷完蛋拉开距离 |
| **被抓** `catch_count` | 乘 `×0.97^次` | 20 层（×0.54） | 治疗 `150×1.25^次` 抹 3 层 | 唯一负面 |

> **设计意图**：**加法源早期猛、乘法源后期猛**。
> 基础速度才 9，买一级鞋 +2.5 就是 +28%，很爽；
> 到速度 90 时 +2.5 只剩 +2.8%，自然被淘汰，玩家自己会转向锻炼。
> 两条曲线在 Lv8 附近交叉。**这比一条线走到底有意思得多。**

### 数值虚假化

内部速度 9–90（物理正确，让怪物 6.0–63.5 有意义），
**显示层统一 ×3.6 换成 km/h**，玩家看到的是「234 km/h【音速】」。

称号：步行 → 慢跑 → 狂奔 → 疾驰 → 音速 → 超音速 → 无视物理

> **原则**：物理判定用内部速度，UI 用显示速度。玩家永远看不到内部数字。

### 起始阶位（去掉"草鸡"）

```gdscript
start_tier_for(speed) = 速度能压过的最高阶（speed >= tier_speed * 1.05）
```
速度 9 → T2 开门；速度 34（Lv14）→ **T7 开门**；满速 → T10。
低阶内容自动跳过，用户明确说过「别出什么草鸡了，谁还要草鸡，还拖慢节奏」。

### 隐蔽 = 苏醒延迟（不是感知范围！）

```gdscript
延迟 = 阶位基础wake × (1 + 0.09 × 隐蔽Lv) × (1 + 0.15 × 披风Lv)
```

> ⚠️ **这是用户亲自诊断出来的**。最初隐蔽是"怪物感知半径"，
> 结果玩家还没偷蛋就被发现，100% 被抓。
> 用户原话："我每次偷蛋都百分百被减速，你知道为什么吗？因为我还没偷他就开始追我了"
> → **删掉了 proximity wake**：睡着的怪不会被路过吵醒，只有蛋被动了才醒。

---

## 六、十阶怪物数值（当前值）

速度 / 体型 / 产出全部**等比**递增（不是等差）：

| 阶 | 名称 | 速度 | 每秒产出 | 苏醒 | 体型 |
|---|---|---|---|---|---|
| T1 | 草鸡 | 6.0 | 0.4 | 1.60s | 0.80 |
| T2 | 林蜥 | 7.8 | 1.2 | 1.35s | 1.05 |
| T3 | 岩龟 | 10.1 | 3.4 | 1.15s | 1.35 |
| T4 | 火蜥 | 13.2 | 9.0 | 0.95s | 1.70 |
| T5 | 冰熊 | 17.1 | 24.0 | 0.80s | 2.10 |
| T6 | 深海鱼龙 | 22.3 | 62.0 | 0.68s | 2.55 |
| T7 | 穴居魔虫 | 28.9 | 160.0 | 0.56s | 3.05 |
| T8 | 高山飞龙 | 37.6 | 420.0 | 0.46s | 3.60 |
| T9 | 云间星龙 | 48.9 | 1100.0 | 0.40s | 4.10 |
| T10 | 宇宙龙 | 63.5 | 2900.0 | 0.35s | 4.60 |

- 速度 **×1.30/阶**（跨度 10.6 倍）· 体型 ×1.21 · 产出 ×2.65
- 玩家 `×1.09/级`，**每深入一阶约需升 2.6 级**，T10 要 Lv21 才跑得过
- 品质 **按跑道长度递增**（不是固定概率）：起点 普通70/优良20/闪光8/变异2 →
  满距 普通25/优良30/闪光30/变异15，平均倍率 ×1.185 → ×1.633。详见 `docs/02`
- 卖价 = 每秒产出 × 25（养着细水长流，卖掉立刻套现）

---

## 七、四轮真人试玩反馈（全部已改）

**完整逐条对照见 `docs/04-试玩反馈与迭代记录.md`**，这里只列结论：

| 轮次 | 用户说的 | 做了什么 |
|---|---|---|
| **1** | 要越肩视角 + 看到远处巨兽；速度要指数乘法；隐蔽应该是苏醒延迟；怪速度要拉开 | 透视相机 offset(1.6,4.4,8.2) pitch −17；`×1.09^Lv` / `×0.97^次`；删 proximity wake；怪速 ×1.30/阶 + 头顶标签 |
| **2** | 还是不膨胀；层次不能等距；丢蛋的怪要追到撤离点 | 怪速改等比；`RUN_TIER_GAPS` 非等距；`_lost_egg` 标志 + **reparent 到 current_scene** |
| **3** | 还是不膨胀；**往回走跑道消失了**；别出草鸡；参考《疯狂动物园》 | 地面虚线固定间距；FOV 72→90；**跑道全程常驻不回收**；起始阶位系统 |
| **4** | 用特效表达速度；数值可以虚假化；提升别那么线性 | 速度线+尘土+抖动+前倾；×3.6 显示 km/h；四源三算法 |
| **5**（快修） | 首个生物离撤离点太远；WASD 一直用不了（只有方向键）；要鼠标自由视角 + **不分局外**，撤离点后面是店铺和养的生物 | `SAFE_DIST` 40→14 + 首块强制生成怪（最近怪 140m → 18m）；新增 `[input]` 自定义动作修 WASD；轨道相机（鼠标转视角 + 滚轮缩放）；**取消场景切换**，`farm_zone.gd` 把六个店铺 + 兽栏建在同一场景的撤离点后方 |
| **6**（快修 2） | ① 人物不会随视角改变前进方向 ② 带蛋回来去卖蛋，说没蛋 ③ 怪物追到撤离点里面来撞我 | ① 移动改成**相对相机**（`move_math.gd` + 8 个断言）② ③ **同一个根因**：追兵能越线 → 提 `SAFE_LINE` 单一常量，追兵过线即回头 + 位置夹取 + 抓取半径卡在线外；跨线时明确提示"已进入撤离区"；卖蛋/入农场牌子同时显示"仓库 N ｜ 农场 M"，空仓库提示区分"在农场产钱"和"真的没有"；自检改为 `SaveManager.test_mode`（**一个字节都不写玩家存档**） |
| **7**（蛋品曲线） | "还是按跑道长度来逐渐出现越来越稀有的蛋吧" | 品质原先是**纯随机、与距离无关**。改成按距离线性插值：起点 普通70/优良20/闪光8/变异2 → 满距 普通25/优良30/闪光30/变异15（平均倍率 +38%）。蛋的外观按品质分档（体积/颜色/自发光/光晕/头顶标签），HUD 第二行直接写当前概率。新增 `tools/quality_test.gd` 统计断言 |

---

## 八、⚠️ 最重要的三条教训（血的教训）

### 教训 1：数值膨胀会被"补偿"抵消掉

**用户连着两轮说"还是没感觉到数值膨胀"**。我把速度从 9 调到 90（×10），却毫无效果。

根因不是数值不够大，**是我在同一轮里做了四件互相抵消的事**：

| 我做的 | 后果 |
|---|---|
| 速度 ×10 | 想让它更快 |
| 世界尺度 ×3.25 | 跑道/间距同比放大 → 相对速度不变 |
| 相机拉远 ×6 | 画面里物体变小、移动变慢 |
| 模型放大 ×2.6 | 参照物变大 → 又抵消一次 |

**净效果 ≈ 白涨。数字涨了，画面没涨，玩家只信画面。**

✅ 正确解法：**固定参照物**。地面横向虚线间距**固定 5 单位，不随世界尺度缩放**——
速度 6 时每秒穿过 1.2 条，速度 63 时每秒 12.7 条，差 10 倍，肉眼直接可见。
相机补偿封顶 3.5（原 6），模型系数 0.15（原 0.35）——补偿只用来防"看不清"，不再抵消速度。

> **接手时如果要调速度感：先问"画面上的参照物经过频率变了吗"，再看数字。**

### 教训 2：纸面平衡一定会翻车，必须让人玩

v1 堆了 160 图鉴条目，零次试玩，用户上来就说方向错了。
v2 里"必被抓"这个**致命设计缺陷**（proximity wake + 读条 freezing 叠加）
是用户玩了一局就诊断出来的，我推算了一整天都没发现。

### 教训 3：动态生成的追兵必须 reparent

丢失蛋的怪物要一路追到撤离点。但 chunk 回收时追兵会被一起删掉，凭空消失。
**必须在 `on_egg_stolen()` 里 reparent 到 `current_scene`**，否则这套机制根本不成立。

同理：单局跑道封顶 `MAX_RUN_DIST = 4000` 且**全程常驻不回收**——
之前"往回走跑道消失"就是因为边跑边回收，回头时路已经没了。

### 教训 4：Godot 内置 `ui_*` 动作**只绑方向键**，不绑 WASD

用户连续反馈"WSAD 一直用不了，用的是右边的上下左右"。

根因：代码写的是 `Input.get_vector("ui_left", "ui_right", "ui_up", "ui_down")`，
而 Godot 4 里 `ui_left/right/up/down` 这几个**内置动作默认只绑方向键**，
`W/A/S/D` 根本不在里面。看起来"移动能用"（方向键能走），所以很难发现。

✅ 正确解法：在 `project.godot` 里自定义 `move_left/right/forward/back` 四个动作，
**同时绑 WASD 的 `physical_keycode`（物理键位，不受键盘布局影响）和方向键的 `keycode`**，
代码改用自定义动作。验证脚本 `tools/input_test.gd` 会打印每个动作的绑定键码。

```
✓ move_left  ← [65, 4194319]   # 65='A'，4194319=LEFT
✓ move_right ← [68, 4194321]
✓ move_forward ← [87, 4194320]
✓ move_back  ← [83, 4194322]
```

> 方向键键码速查：LEFT=4194319 / UP=4194320 / RIGHT=4194321 / DOWN=4194322。

### 教训 5：自检脚本**不能污染玩家存档**

`--shop` 自检本来是为了验证商店，结果它**真的调用了购买逻辑**，
`_after_buy()` 里又调 `SaveManager.save()`，一跑完用户的存档就被写成
`coins: 999479.0`、`farm: [...]` 一堆测试数据。

我第一版的修法是"**开始时快照、结束时还原**"。这不够——
中途报错、进程被杀、或者玩家**正在同时玩**，都会让还原没机会执行。

✅ 最终解法：**`SaveManager.test_mode`**。自检一开始就打开，
此后 `save()` 直接 `return`，**一个字节都不会写盘**。

```gdscript
# scripts/core/save_manager.gd
var test_mode := false

func save() -> void:
	if test_mode:
		return
	...
```

> **规则**：只要自检会碰到"会存档"的代码路径（购买、撤离、被抓都会），
> 就必须先 `SaveManager.test_mode = true`。
> **跑完对着存档文件 `diff` 一下**，确认逐字节没变——这是唯一可信的验证。
>
> ⚠️ 另外注意：如果用户**正在玩**，你跑任何 headless 测试都要小心。
> 现在自检不写档了，所以是安全的；但别在测试里手工改存档文件。

### 教训 6：玩家报的两个症状，可能是同一个根因

用户一次性报了三条，其中两条听起来毫不相干：

> "我把蛋带回来了，然后我去卖蛋，**他说我没有蛋**"
> "**怪物都追到撤离点里面来撞我了**"

我一开始只查"卖蛋"那段代码，写了 `--esc` 自检把链路每一环都打出来，
结果是 **`卖掉 2 颗，到手 84 金币`——完全正常**。

真正的原因是第二条：**追兵能跟着玩家越过撤离线**。
所以玩家在过线前最后一米被撞掉蛋 → 背包空了 → 过线时没东西入库 →
到摊位自然说"仓库是空的"。**"卖蛋说没蛋"只是"怪追进家"的下游症状。**

**教训**：不要按用户给的**症状编号**逐个修，要去找**共同的上游**。
一条自检链路（`--esc`）把"哪一环真的断了"钉死，比读十遍代码有用。

另外值得记的是：**存档文件本身就是最好的现场证据**。
我读了一下 `egg_runner_save.json`，看到 `stored` 里明明躺着 1 颗蛋、
`farm` 里有 1 只，立刻就排除了"入库代码坏了"这个方向。
**遇到"我明明做了 X，它说我没做"的反馈，先去看持久化文件里到底有什么。**

### 教训 7：同一个概念写在三个地方 = 一定会漏改

撤离线 `-12` 在三个地方各写了一遍（蛋入库 / 追兵放弃 / 位置夹取），
结果就是"蛋已经进仓库了，怪却还追进农场撞人"。

✅ 解法：**提成 `CreatureDB.SAFE_LINE` 一个常量**，三处都引用它。

> **信号**：如果你发现自己在两个以上文件里写同一个数字/字符串，
> 停一下，把它提成一个常量。**这次是第三次因为这件事出 bug 了。**

---

## 九、没做完的 / 已知问题

### 已完成的

**P0-1 每阶怪物外形差异化** ✅ —— 十阶各有一套独立的身体蓝图（`scripts/run/creature_model.gd`）：
两足鸡 / 四足低伏蜥 / 圆壳龟 / 直立棘龙 / 厚重熊 / 无腿浮游鱼龙 / 分节蠕虫 /
大翼飞龙 / 蛇形星龙 / 三头宇宙龙。腿数、翼、壳、分节都是结构性差异，剪影就能分开。

用 `--gallery` 可以逐阶查看（按 `1`-`9`、`0` 跳阶）。详见 `docs/08`。

**P0-2 农场可视化** ✅ —— 撤离点后建了真实兽栏，养的怪以模型形式在里面漫步（详见第五节、`docs/09`）。
不再只是 UI 列表。

**快修三项** ✅ —— 首怪距离（140m→18m）、WASD 绑定、鼠标自由视角 + 取消场景切换。

### 明确的待办

1. **⚠️ 被抓惩罚会把玩家打回 T1「草鸡」（新发现，建议优先修）**
   `start_tier_for(speed)` 只看**当前**速度。当前存档被抓 7 次 →
   速度 `9 × 0.97^7 = 7.27`，低于 T2 门槛 8.19 → **起始阶位掉回 T1**。
   而用户明确说过「别出什么草鸡了，谁还要草鸡，还拖慢节奏」——
   **惩罚不该让玩家重新看见已经淘汰的内容。**
   修法建议：起始阶位取 `max(按当前速度算, 按历史最高速度算)`，
   或者干脆给一个下限（最低 T2）。**这是设计问题，不是 bug，改前最好问用户。**
2. **宇宙龙的终极目标感** —— T10 现在有三头 + 光环了，
   但还缺一块"**怎么拿到它**"的玩法牵引。v1 里用户要求过「开局就把宇宙龙摆在眼前」，
   目前只做到"远处能看见剪影"
3. **图鉴系统** —— v1 有，v2 还没搬过来
4. **音效** —— 完全没有
5. **农场区的深度** —— 现在店铺/兽栏只是"能看能用"，
   兽栏里还看不出**每只怪分别产多少钱**（只显示总数），可以考虑加头顶浮动数字

### 已知问题

- **存档被写过两次**：
  - 一次是**我自己的 `--shop` 自检**把存档写成 `coins: 999479`（已修：加快照还原，见教训 5）
  - 另一次更早：用户进度一度是 `Lv14 / 9155 金币 / 农场 36 只`，某次变成全 0。
    已向用户确认是否误触「清空存档」，**尚未得到答复**。
  存档在 `user://egg_runner_save.json`（Windows 实路径见 `docs/05`）。
  **接手时建议先读一下存档文件确认现状。**
- 高速时（>60）粒子 + 抖动可能有点晕，尚未收到反馈
- 远处睡怪靠 `visible=false` + 跳过 `_physics_process`（>700 单位）来省性能，
  80 只怪时勉强够用，怪再多会掉帧
- 兽栏怪 >20 只时只显示前 20 只（`MAX_PEN_CREATURES`），是**显示**上限不是数据上限

### 自行拍板、需要用户追认的设计

1. **永久降速封顶 20 层（×0.54）+ 可花钱治疗** —— 字面永久会锁死玩家
2. **撤离必须跑回起点** —— 否则"跑得深"没有风险。备选：沿途撤离门 / 偷到即算
3. **被抓只掉最贵 1 颗，不是全部** —— 全掉太劝退
4. **单局跑道 4000 m 封顶 + 尽头墙** —— 为了修 bug 顺带定的，可能太长/太短

---

## 十、给接手 AI 的建议

### 动手前

1. 先完整读 `docs/06-技术陷阱清单.md` —— 里面的坑会浪费你一小时
2. 先 `--headless` 跑一遍确认基线是干净的，再改代码
3. **任何数值改动，改完立刻让用户玩一局**。别推算，推算会翻车

### 如果用户说"还是没感觉"

先做这个诊断，按顺序排查：

1. 地面虚线的**通过频率**变了吗？（主来源，没变就是白调）
2. 相机补偿是不是又把速度抵消了？（检查 `camera_zoom` 封顶 3.5）
3. 世界尺度是不是跟着一起放大了？（`world_scale` 会抵消速度）
4. 数字涨了但玩家看不到？（检查是不是没走 `get_display_speed()`）

### 不要做的事

- ❌ 不要为了"内容量"堆阶位/品种 —— v1 就是这么死的
- ❌ 不要同时调"速度 + 世界尺度 + 相机 + 模型"这四样（见教训 1）
- ❌ 不要动 `DISPLAY_SPEED_SCALE` 来修物理问题（那是纯显示层）
- ❌ 不要用 `ParticleProcessMaterial` 写 `CPUParticles3D`（会静默失效，见 docs/06）

---

## 十一、关键常量速查

```gdscript
# scripts/core/game_state.gd
BASE_SPEED          = 9.0       # 基础速度
TRAIN_GROWTH        = 1.09      # 锻炼：每级 ×1.09
MAX_LEVEL           = 30
SHOE_BONUS          = 2.5       # 跑鞋：每级 +2.5
MAX_SHOE            = 20
MAX_GEAR            = 5
CLOAK_PER_LEVEL     = 0.15      # 披风：每级 +15% 苏醒延迟
JET_PER_LEVEL       = 0.25      # 喷气：每级 +25%
JET_DURATION        = 2.5       # 喷气只在前 2.5 秒
PENALTY_GROWTH      = 0.97      # 被抓：每次 ×0.97
MAX_CATCH_STACK     = 20        # 封底 ×0.54
DISPLAY_SPEED_SCALE = 3.6       # 虚假化：内部→km/h
GRAB_TIME           = 1.2       # 偷蛋读条
WAKE_DELAY_PER_LEVEL= 0.09      # 隐蔽：每级 +9% 苏醒延迟

# scripts/data/creature_db.gd
MAX_RUN_DIST        = 4000.0    # 单局跑道封顶
SAFE_LINE           = -12.0     # ★ 撤离线 / 安全线（三处共用，只改这里）
RUN_TIER_GAPS       = [250, 600, 1200, 2200, 3800, 6300, 10000, 16000]
WORLD_SCALE_PER_TIER= 0.25      # T10 → ×3.25
# camera_zoom   = clamp(1 + 速度/25, 1, 3.5)
# model_scale   = 1 + (zoom-1) * 0.15

# scripts/run/farm_zone.gd
HALF_W           = 26.0     # 农场区半宽
STATION_Z        = 10.0     # 店铺摊位 z
PEN_Z            = 46.0     # 兽栏中心 z
PEN_R            = 15.0     # 兽栏半径
Z_FAR            = 74.0     # 农场区尽头
INTERACT_RANGE   = 5.0      # 交互距离
MAX_PEN_CREATURES= 20       # 兽栏显示上限
# 六个摊位 x：-18 / -10.5 / -3 / 4.5 / 12 / 19.5

# scripts/core/main.gd（轨道相机）
CAM_TARGET_Y   = 1.3
CAM_DIST_DEF   = 8.6        # 默认距离
CAM_DIST_MIN/MAX = 3.0 / 24.0
CAM_PITCH_DEF  = 0.30
CAM_PITCH_MIN/MAX = -0.35 / 1.15
MOUSE_SENS     = 0.0026
WHEEL_STEP     = 0.9
# camera_pos = target + (sin(yaw)cos(pitch), sin(pitch), cos(yaw)cos(pitch)) * dist

# scripts/run/track.gd
SAFE_DIST      = 14.0       # 撤离点安全区（快修：原 40.0）
# 首块 chunk 额外强制生成一只怪，z = -(SAFE_DIST + 4.0) = -18

# scripts/run/player.gd（农场区边界）
FARM_HALF_W    = 24.0
FARM_END_Z     = 71.0
```

---

## 十二、交接时的存档快照

2026-09-12 23:05 检查时（**用户正在玩**）：

```json
{"best_distance":162.558181762695, "catch_count":10, "coins":39.0,
 "farm":[{"income":1.2,"name":"林蜥蛋","quality_mult":1.0,"quality_name":"普通","tier":2}],
 "gear_cloak":0, "gear_jet":0, "shoe_level":1, "speed_level":1, "stealth_level":0,
 "stored":[], "total_caught":10, "total_eggs":9}
```

含义：速度 Lv1、跑鞋 Lv1、被抓 10 次、39 金币、**农场里有 1 只林蜥蛋在产钱**。
最远跑到 162.6 m，累计偷到 9 颗蛋。

> **这份数据同时是"蛋没丢"的证据**：`farm` 里有 1 只、`stored` 空，
> 说明入库和入农场的代码都是好的。用户抱怨的"卖蛋说没蛋"
> 是**追兵在过线前把蛋撞掉**造成的（见教训 6）。
>
> 存档目录里有一个备份：`egg_runner_save.json.bak-20260912-230150`。
> 接手时如果要改数值，**先自己再备份一份**，别直接覆盖。

### "进度归零"的根因（2026-09-12 已定位并修复）

早先记录过的进度是 **速度 Lv14 / 9155 金币 / 农场 36 只**，后来变成全 0。
当时怀疑是误触，**其实不是** —— 是存档路径会漂移：

1. 本机启动环境里 **`APPDATA` 是空的**（`tools/path_probe.gd` 可验证）。
2. Godot 的 `OS.get_user_data_dir()` 是拿 `APPDATA` 拼出来的，
   空了之后就退化成**相对路径** `./Godot/app_userdata/偷蛋长跑`。
3. 相对路径由**进程工作目录**决定 —— 换个目录启动，就是另一份（空的）存档。

所以"进度归零"= 换了个方式/目录启动，读到了另一份空档。**旧的 Lv14 存档大概率还在**，
只是躺在另一个工作目录下的 `Godot/app_userdata/偷蛋长跑/` 里。

**修复**：`SaveManager.save_path()` 不再用 `user://`，而是显式取
`ProjectSettings.globalize_path("res://")` 拼绝对路径（导出成 `.pck` 后才退回 `user://`）。
`play.cmd` / `editor.cmd` 也加了 `cd /d "%~dp0"` 固定工作目录。

**回归方式**：从两个不同的工作目录各跑一次 `tools/path_probe.gd`，
两次打印的存档路径必须完全一致。

存档路径（本便携包的实路径，**不是**常规的 `%APPDATA%`）：
```
<仓库根>/Godot/app_userdata/偷蛋长跑/egg_runner_save.json
```

---

## 十三、怎么自己启动这个游戏

**为什么之前找不到**：这个工程是直接写文件建出来的，从没被编辑器导入过，
所以不在 Godot 项目管理器列表（`%APPDATA%/Godot/projects.cfg`）里。
现在已把工程路径登记进 `projects.cfg`，打开 Godot 就能在项目列表里看到。

工程根目录下有两个双击即可的批处理：

| 文件 | 作用 |
|---|---|
| `play.cmd` | 直接开始玩（无编辑器窗口） |
| `editor.cmd` | 用编辑器打开工程（也会把它登记进项目列表） |

两个都用 `%~dp0` 取自身所在目录，**不会被路径里的空格截断**。
手工命令行启动时同样要注意：路径含空格，**必须加引号**，
否则 Godot 会打印 `Invalid project path specified` 然后**瞬间退出**（看着就像"闪退"）。

---

*最后更新：2026-09-12 23:35 · 由 WorkBuddy AI 整理 · 对应"存档路径锚定 + 启动方式说明"之后*
